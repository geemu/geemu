server:
  port: 8761

eureka:
  instance:
    prefer-ip-address: true
    ip-address: 47.97.215.198
  server:
    enable-self-preservation: false
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://cloud.chenfangming.com:${server.port}/eureka
