server:
  port: 8761

eureka:
  instance:
    prefer-ip-address: true
  server:
    enable-self-preservation: false
  client:
    fetch-registry: false # 指示此客户端是否应从Eureka获取Eureka注册表信息
    register-with-eureka: false # 指示此实例是否应向Eureka注册其信息
    service-url:
      defaultZone: http://127.0.0.1:${server.port}/eureka
