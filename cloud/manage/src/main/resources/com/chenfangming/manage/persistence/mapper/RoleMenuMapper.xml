<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chenfangming.manage.persistence.mapper.RoleMenuMapper">

    <resultMap id="BASE_RESULT_MAP" type="com.chenfangming.manage.persistence.entity.RoleMenuEntity">
        <id column="id" property="id"/>
        <result column="role_id" property="roleId"/>
        <result column="menu_id" property="menuId"/>
    </resultMap>

    <select id="selectByRoleIdList" resultMap="BASE_RESULT_MAP">
        select *
        from backend_user_role
        where user_id IN
        <foreach collection="list" open="(" separator="," close=")" item="item" index="index">
            #{item}
        </foreach>
    </select>

</mapper>
