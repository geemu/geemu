spring:
  main:
    allow-bean-definition-overriding: true
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      name: user

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka/