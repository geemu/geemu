spring:
  application:
    name: cloud-config
  profiles:
    active: native
    include: @springProfile@
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config/@springProfile@
