<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chenfangming.manage.persistence.mapper.UserMapper">

    <resultMap id="USER_RESULT_MAP" type="com.chenfangming.manage.persistence.entity.UserEntity">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="password" property="password"/>
        <result column="remark" property="remark"/>
        <result column="enabled" property="enabled"/>
        <result column="create_user" property="createUser"/>
        <result column="create_time" property="createTime"/>
        <result column="update_user" property="updateUser"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="findByName" resultMap="USER_RESULT_MAP">
        select *
        from backend_user u
        where u.name = #{name}
    </select>

    <insert id="insert" useGeneratedKeys="true">
        insert into backend_user(name, password, remark, enabled, create_user, update_user)
        values (#{item.fundName},
                #{item.fundCode},
                #{item.dateX},
                #{item.dataY},
                #{item.createBy},
                #{item.createDate},
                #{item.updateBy},
                #{item.updateDate},
                #{item.remarks},
                #{item.delFlag})
    </insert>


</mapper>
