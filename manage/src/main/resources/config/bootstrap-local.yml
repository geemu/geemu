spring:
  main:
    allow-bean-definition-overriding: true
  cloud:
    config:
      discovery:
        enabled: true
        service-id: cloud-config
      name: application-manage
      profile: local

eureka:
  client:
    service-url:
      defaultZone: http://eureka.vultr.chenfangming.com:8761/eureka,http://eureka.aliyun.chenfangming.com:8761/eureka
